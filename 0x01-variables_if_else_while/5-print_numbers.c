#include <stdio.h>
/**
 * main - It's the main function
 * Return:0, should always be the return
 */

int main(void)
{
	int y;

	for (y = 0; y < 10; y++)
		printf("%d", y);
	printf("\n");
	return (0);
}
