#include "main.h"
/**
 * _isdigit - function checking for a digit 0 through 9
 * @c: function parameter
 * Return: 1 if successful or 0 on failure
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
