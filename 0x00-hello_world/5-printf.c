#include <stdio.h>
/**
 * main - Creating a C program that prints a sentence using printf function
 * Return: 0 (If the return is 0, then the code is sucessful)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
