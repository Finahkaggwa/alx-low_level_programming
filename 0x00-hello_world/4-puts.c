#include <stdio.h>
/**
 * main - Creating a C program that prints a sentence using the puts function
 * Return: 0 (If the return is 0 then its successful)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}

